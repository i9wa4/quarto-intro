# zizmor configuration
# https://docs.zizmor.sh/usage/

rules:
  artipacked:
    # We need persist-credentials for quarto publish to push to gh-pages
    # The workflow only runs on trusted code (main branch push only)
    # and permissions are already restricted to contents: write
    ignore:
      - "publish.yml"
